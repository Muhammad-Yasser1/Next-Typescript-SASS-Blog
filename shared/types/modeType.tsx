export type modeStateType = {
	mode: {
		activeMode: 'admin' | 'reader';
		validModes: ['admin', 'reader'];
	};
};

export type modeObjType = {
	activeMode: 'admin' | 'reader';
	validModes: ['admin', 'reader'];
};
